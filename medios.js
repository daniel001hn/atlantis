/* Generado por la Edge Function de Supabase - no editar a mano.
 *
 * Dice que espacios tienen una foto o video puestos desde admin.html. Lo que
 * no este aca usa el archivo que viene en el repo, asi que si este archivo
 * falta o llega vacio el sitio se ve igual que siempre.
 *
 * Es .js y no .json a proposito, por dos razones:
 *   - un fetch() desde file:// falla por CORS, y el index se abre con doble
 *     clic todo el tiempo para revisarlo; un <script src> anda igual
 *   - sigue el mismo patron que disponibilidad.js y stats.js
 */
window.MEDIOS = {
  "actualizado": null,
  "slots": {}
};
